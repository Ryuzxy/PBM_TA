"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.startFlutter = void 0;
const server_1 = require("@genkit-ai/tools-common/server");
const utils_1 = require("@genkit-ai/tools-common/utils");
const commander_1 = require("commander");
const get_port_1 = __importStar(require("get-port"));
const open_1 = __importDefault(require("open"));
const manager_utils_1 = require("../utils/manager-utils");
exports.startFlutter = new commander_1.Command('start:flutter')
    .description('runs a flutter app in Genkit dev mode')
    .option('-n, --noui', 'do not start the Dev UI', false)
    .option('-p, --port <port>', 'port for the Dev UI')
    .option('-o, --open', 'Open the browser on UI start up')
    .option('--disable-realtime-telemetry', 'Disable real-time telemetry streaming')
    .option('--cors-origin <origin>', 'specify the allowed origin for CORS requests')
    .action(async (options) => {
    const projectRoot = await (0, utils_1.findProjectRoot)();
    if (projectRoot.includes('/.Trash/')) {
        utils_1.logger.warn('It appears your current project root is in the trash folder. ' +
            'Please make sure that you current working directory is correct.');
    }
    const { envVars, reflectionV2Port, telemetryServerUrl } = await (0, manager_utils_1.getDevEnvVars)(projectRoot, {
        ...options,
        experimentalReflectionV2: true,
    });
    const dartDefines = Object.entries(envVars).map(([key, value]) => `--dart-define=${key}=${value}`);
    const flutterArgs = ['run', ...dartDefines, ...exports.startFlutter.args];
    const { manager, processPromise } = await (0, manager_utils_1.startDevProcessManager)(projectRoot, 'flutter', flutterArgs, {
        ...options,
        experimentalReflectionV2: true,
        envVars,
        reflectionV2Port,
        telemetryServerUrl,
    });
    if (!options.noui) {
        let port;
        if (options.port) {
            port = Number(options.port);
            if (isNaN(port) || port < 0) {
                utils_1.logger.error(`"${options.port}" is not a valid port number`);
                return;
            }
        }
        else {
            port = await (0, get_port_1.default)({ port: (0, get_port_1.makeRange)(4000, 4099) });
        }
        (0, server_1.startServer)(manager, port);
        if (options.open) {
            (0, open_1.default)(`http://localhost:${port}`);
        }
    }
    await processPromise;
});
//# sourceMappingURL=start-flutter.js.map