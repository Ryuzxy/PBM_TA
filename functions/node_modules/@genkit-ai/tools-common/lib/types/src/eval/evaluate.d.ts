import type { BaseRuntimeManager } from '../manager/manager';
import { type Action, type Dataset, type EvalInput, type EvalKeyAugments, type EvalRun, type EvalRunKey, type RunNewEvaluationRequest } from '../types';
export declare function runNewEvaluation(manager: BaseRuntimeManager, request: RunNewEvaluationRequest): Promise<EvalRunKey>;
export declare function runInference(params: {
    manager: BaseRuntimeManager;
    actionRef: string;
    inferenceDataset: Dataset;
    context?: string;
    actionConfig?: any;
}): Promise<EvalInput[]>;
export declare function runEvaluation(params: {
    manager: BaseRuntimeManager;
    evaluatorActions: Action[];
    evalDataset: EvalInput[];
    augments?: EvalKeyAugments;
    batchSize?: number;
}): Promise<EvalRun>;
export declare function getAllEvaluatorActions(manager: BaseRuntimeManager): Promise<Action[]>;
export declare function getMatchingEvaluatorActions(manager: BaseRuntimeManager, evaluators?: string[]): Promise<Action[]>;
